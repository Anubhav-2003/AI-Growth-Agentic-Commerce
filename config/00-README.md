# Application Configuration

This folder contains non-secret, shared configuration consumed by the CommerceOS runtime.

- `commerce.yml` — route prefixes, collection names, limits, aliases, UCP metadata, and user-facing copy.

Secrets and deployment-specific values belong in `.env` or process environment variables, not here.
