# Small CSV Fixture

This folder contains the compact CSV source used for quick local workflows and basic normalization checks.

- `products.csv` — a ten-product catalog with representative product, pricing, stock, and rating fields.

Keep this fixture stable when possible because the README's example synchronization command uses its path.
